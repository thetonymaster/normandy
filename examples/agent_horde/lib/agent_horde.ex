defmodule AgentHorde do
  @moduledoc """
  AgentHorde — a multi-agent pipeline demo built on Normandy.

  This module is the top-level entry point. Tools, agents, and the
  orchestration pipeline are defined in sub-modules (added in later tasks).
  """
end
